# NORME for projects:

## NAMING OF VARIABLES:

- variables that represent either a list, an array or so, should be named and
  end with an 's' (plural), so that we know it can contain more than one
  element.
	example:
		int number;
		int numbers[];
